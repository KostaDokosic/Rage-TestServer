// place id's of players to send this script to
executeTo = [0]

//mp.gui.chat.push('hotloader is working');
let player = mp.players.local;
mp.gui.chat.push(player.getNumberOfPropTextureVariations(1, 1) + '');